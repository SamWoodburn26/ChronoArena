# ---------------------------------------------------------------
# ChronoArena  —  runtime configuration
# Copy this file to the same directory as the jar/executable.
# ---------------------------------------------------------------

# --- Network ---
serverIP=localhost
TCP_port=1234
UDP_port=1235

# --- Identity (client only) ---
player_name=Player1

# --- Map ---
map_width=900
map_height=650

# --- Round ---
round_duration_sec=180

# --- Zones ---
zone_count=4
zone_capture_sec=3
zone_capture_contest_sec=6
zone_grace_sec=5
zone_points_per_sec=5

# --- Items ---
item_max_active=4
item_spawn_interval_ticks=20
item_pickup_radius=30
energy_item_points=25

# --- Freeze-ray weapon ---
freeze_duration_sec=4
freeze_cooldown_sec=8
freeze_weapon_ttl_sec=30
freeze_penalty_points=10
